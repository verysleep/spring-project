package org.zerock.sample;

import org.springframework.stereotype.Component;

import lombok.Data;

// 자동생성을 위한 어노테이션
// - @Controller(uri:HTML), @Service(처리), @Repository(데이터 저장)
// - @Component(구성체), @RestContoller(uri:data:ajax), @~Adivce(예외 처리)
@Component
@Data
public class Chef {
	
}
